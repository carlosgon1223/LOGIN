  const PGUSER='postgres' 
  const PGHOST='localhost'
  const PGPORT = '5433'
  const PGDATABASE='Registro'

  module.exports ={
      PGUSER,
      PGHOST,
      PGPASSWORD,
      PGDATABASE,
      PGPORT
  }